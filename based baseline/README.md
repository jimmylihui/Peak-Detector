# Based Baseline

Clean baseline-method code copied from the original benchmark tree.

This folder keeps method implementations and dataset test entrypoints only. Generated arrays, model checkpoints, prediction files, plots, result tables, cache folders, scaling experiments, cross-modality experiments, and relative-tolerance artifact files are excluded.

The data arrays consumed by these scripts are generated from `data processing`.
